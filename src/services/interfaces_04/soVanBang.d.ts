declare module SoVanBang {
	export interface Record {
		nam: number; // Năm sổ văn bằng
		soVaoSo: number; // Số vào sổ, tự động tăng dần
		sosoCuoi: number; // Số hiệu văn bằng cuối cùng đã cấp trong sổ này
	}
}
